#ifndef H_STACK
#define H_STACK

typedef struct element element_s;
typedef struct stack stack_s;
typedef struct coord coord_s;

stack_s *initialize();
void stack_push (stack_s *, coord_s);
coord_s stack_pop (stack_s *);
void stack_print(stack_s *);

#endif /* not H_STACK */